<launch>

  <arg name="uav_name" default="$(optenv UAV_NAME uav)"/>
  <arg name="standalone" default="$(optenv STANDALONE false)"/>
  <arg name="profiler" default="$(optenv PROFILER false)" />

  <arg unless="$(arg standalone)" name="nodelet" value="load"/>
  <arg     if="$(arg standalone)" name="nodelet" value="standalone"/>
  <arg unless="$(arg standalone)" name="nodelet_manager" value="$(arg uav_name)_nodelet_manager"/>
  <arg     if="$(arg standalone)" name="nodelet_manager" value=""/>

  <group ns="$(arg uav_name)">

    <node pkg="nodelet" type="nodelet" name="rtk_republisher" args="$(arg nodelet) mrs_odometry/RtkRepublisher $(arg nodelet_manager)" output="screen">

      <rosparam file="$(find mrs_odometry)/config/rtk_republisher.yaml" />
      <param name="enable_profiler" value="$(arg profiler)" />

      <!-- Subscribers -->
      <remap from="~odom_in" to="ground_truth" />
      <remap from="~tersus_in" to="tersus/bestpos" />
      <remap from="~rtk_out" to="rtk_gps/global" />

      <!-- Publishers -->
      <remap from="~profiler" to="profiler" />

    </node>

  </group>

</launch>
